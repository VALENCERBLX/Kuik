[package]
name = "kr3ative/kuik"
version = "2.0.0"
description = "A Roblox UI library in the hairline-rule style: rules and type, no panels, springs throughout."
license = "MIT"
authors = ["kr3ative"]
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

[dependencies]
# None. Kuik ships its own motion system, so consuming it pulls in one thing.
